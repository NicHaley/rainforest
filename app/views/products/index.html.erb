<h1>Products</h1>
<%= link_to "Add a Product", "/products/new" %>

<% @products.each do |product| %>
	<p><%= link_to "#{product.name}", "/products/#{product.id}" %> | Price: $<%= product.formatted_price %></p>
	<p><%= product.description %></p>
<% end %>